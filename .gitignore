.venv/
__pycache__/
*.egg-info/
.DS_Store
MUJOCO_LOG.TXT
./docs/robotis/*
runs/
traces/
# Rendered clips. analysis/wheel_slowmo.py writes to traces/ by
# default; these were briefly tracked in analysis/ at ~7 MB each.
analysis/*.mp4
# Regenerated by `python -m aow_sim.export_deploy`, and pinned to a params
# digest that hw.state checks on load -- a stale one raises rather than
# silently flying the wrong gains.
deploy/
